// Generated file. Do not edit.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: "com.android.library"
apply plugin: "kotlin-android"
apply plugin: "maven-publish"

android {
    namespace = "com.myphsar.myphsar_module"
    compileSdk = 35
    defaultConfig {
        minSdk = 21
        targetSdk = 35
    }
}
// Add this at the end of the file
afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = 'com.github.sak-coder'
                artifactId = 'myphsar_module'
                version = '1.0.2'
            }
        }
    }
}
// Add this task to build the AAR
task buildAar(type: Exec) {
    workingDir '../'
    commandLine 'flutter', 'build', 'aar', '--no-profile'
}

// Optional: Clean task
task cleanAar(type: Exec) {
    workingDir '../'
    commandLine 'flutter', 'clean'
}

// Make clean depend on cleanAar
clean.dependsOn(cleanAar)
